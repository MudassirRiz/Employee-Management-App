# Employee Management Project (MERN Stack)

## 📌 Project Overview

The Employee Management Project is a **MERN Stack application** used to manage employee records.
It supports complete **CRUD operations** (Create, Read, Update, Delete) for efficient data handling.

## 🛠️ Tech Stack (MERN)

* **MongoDB** – Database
* **Express.js** – Backend framework
* **React.js** – Frontend library
* **Node.js** – Runtime environment

## ⚙️ Features (CRUD Operations)

* **Create** new employee records
* **Read** and view employee details
* **Update** existing employee information
* **Delete** employee records

## 📦 Install Dependencies

Install all required dependencies using:

```
npm install
```

## ▶️ Run the Project

Start the project using:

```
npm start
```

## 🔐 Environment Variables

* The `.env` file contains sensitive data (database URL, ports, etc.)
* **The `.env` file is not pushed to GitHub for security reasons**
* Create a `.env` file manually in the root directory

### Example `.env`

```
PORT=3000
MONGO_URI=your_database_url
```

## 📂 Important Notes

* `.env` file is added to `.gitignore`
* `node_modules` is not pushed to GitHub
* This project is created for learning and educational purposes and to check my true knowledge

## 👨‍💻 Author

Mudassir Rizvi (mdssrrizvi@gmil.com)
